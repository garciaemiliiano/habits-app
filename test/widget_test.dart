// Placeholder. La app real requiere init de sqflite/tz/notifications.
// Tests reales van en /test cuando se sumen casos puros (streak_calculator,
// HabitFrequency.isDueOn, etc.).
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
